openapi: 3.1.0
info: { title: Orders API, version: 1.0.0 }
paths:
  /v1/orders:
    post:
      responses:
        "201": { description: created }
  /v1/orders/{orderId}:
    get:
      responses:
        "200": { description: found }

