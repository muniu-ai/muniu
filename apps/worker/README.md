# @mn/worker

Candidate orchestration, Gates, repair loops and sandbox backends.

## Commands

```bash
npm run build -w @mn/worker
npm run typecheck -w @mn/worker
npm run test -w @mn/worker
```

This package is private and versioned with the Muniu monorepo.
