image:
  repository: ghcr.io/muniu-ai/muniu
  tag: 0.1.0
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""
  automountServiceAccountToken: false

api:
  replicas: 2
  host: 0.0.0.0
  port: 7318
  resources:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: "2", memory: 2Gi }

worker:
  # Enable after supplying the machine JWT and a shared PVC. Each claim gets a
  # separate candidate Pod; fixtureMode only swaps the model executor for the
  # deterministic acceptance implementation.
  enabled: false
  fixtureMode: false
  replicas: 2
  capacity: 1
  claimTtlMs: 30000
  resources:
    requests: { cpu: 250m, memory: 512Mi }
    limits: { cpu: "2", memory: 2Gi }
  authExistingSecret: muniu-worker-auth
  authTokenKey: token

corsAllowlist: []
enterpriseProjectRoots: ["/workspaces"]

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: muniu.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []

autoscaling:
  api:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
  worker:
    enabled: false
    minReplicas: 2
    maxReplicas: 20
    targetCPUUtilizationPercentage: 70

podDisruptionBudget:
  enabled: true
  minAvailable: 1

networkPolicy:
  enabled: true
  ingressNamespaceSelector:
    matchLabels:
      kubernetes.io/metadata.name: ingress-nginx
  dnsNamespaceSelector:
    matchLabels:
      kubernetes.io/metadata.name: kube-system
  # Standard NetworkPolicy egress rules for PostgreSQL, S3, OIDC/JWKS and
  # OTLP destinations. Keep this explicit because policies cannot allow DNS
  # names portably. Production values must narrow these selectors/CIDRs.
  apiEgress: []
  # Kubernetes API ClusterIP CIDR(s), normally a single /32. Both the API
  # authority and Worker controller need port 443; candidate Pods never do.
  kubernetesApiEgress: []

securityContext:
  runAsNonRoot: true
  runAsUser: 10001
  runAsGroup: 10001
  fsGroup: 10001
  seccompProfile: { type: RuntimeDefault }

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities: { drop: ["ALL"] }

postgres:
  existingSecret: muniu-postgres
  urlKey: url

s3:
  endpointUrl: ""
  bucket: ""
  region: us-east-1
  prefix: muniu
  existingSecret: muniu-s3
  accessKeyIdKey: access-key-id
  secretAccessKeyKey: secret-access-key
  sessionTokenKey: session-token
  kmsKeyId: ""

oidc:
  issuer: ""
  audience: muniu
  jwksUrl: ""

otlp:
  endpoint: ""
  serviceName: mn-api

standardPackTrust:
  existingSecret: muniu-standard-pack-trust
  key: trust.json

sandbox:
  driver: kubernetes
  image: ghcr.io/muniu-ai/muniu-sandbox:0.1.0
  imageDigest: ""
  attestationExistingSecret: muniu-sandbox-attestation
  attestationKey: key
  serviceAccountName: ""
  automountServiceAccountToken: false
  runtimeClassName: ""
  networkPolicy: default-deny
  hostPathAllowed: false
  sharedWorkspace:
    create: true
    existingClaim: ""
    storageClassName: ""
    accessModes: [ReadWriteMany]
    size: 20Gi
    mountPath: /work/sandboxes

vault:
  address: ""
  role: ""
  annotations: {}

extraEnv: []
nodeSelector: {}
tolerations: []
affinity: {}
