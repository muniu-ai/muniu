# 安全策略

v0.1.x 是开发者预览版，只支持最新补丁版本。请通过 `muniu-ai/muniu` 的 GitHub 私密漏洞报告提交问题，不要在公开 issue 中放入利用细节、secret 或用户数据。

动态 Cordis 插件、可执行 YAML/JS 配置和 HMR 是可信代码机制，不是权限沙箱。插件可以访问宿主进程可见的凭据、文件、网络和进程能力，必须由管理员固定版本、验证完整性并审查来源。

Agent 的文件、进程和 Git 副作用必须经过工作区边界、策略、审批、能力、超时和审计。企业会话使用 PostgreSQL CAS 与 S3 对象摘要验证；发现对象缺失或篡改时失败关闭。
