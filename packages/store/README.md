# @mn/store

Local file/SQLite stores and secret vault.

## Commands

```bash
npm run build -w @mn/store
npm run typecheck -w @mn/store
npm run test -w @mn/store
```

This package is private and versioned with the Muniu monorepo.
