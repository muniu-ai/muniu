# @mn/agent-protocol

Versioned Agent messages, bindings, events and transport DTOs.

## Commands

```bash
npm run build -w @mn/agent-protocol
npm run typecheck -w @mn/agent-protocol
npm run test -w @mn/agent-protocol
```

This package is private and versioned with the Muniu monorepo.
