# @mn/provider-catalog

Provider/model catalog and application projection.

## Commands

```bash
npm run build -w @mn/provider-catalog
npm run typecheck -w @mn/provider-catalog
npm run test -w @mn/provider-catalog
```

This package is private and versioned with the Muniu monorepo.
