# @mn/agent-host

Agent lifecycle host and session runner.

## Commands

```bash
npm run build -w @mn/agent-host
npm run typecheck -w @mn/agent-host
npm run test -w @mn/agent-host
```

This package is private and versioned with the Muniu monorepo.
