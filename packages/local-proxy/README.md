# @mn/local-proxy

Provider proxy, usage accounting and receipts.

## Commands

```bash
npm run build -w @mn/local-proxy
npm run typecheck -w @mn/local-proxy
npm run test -w @mn/local-proxy
```

This package is private and versioned with the Muniu monorepo.
