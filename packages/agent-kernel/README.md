# @mn/agent-kernel

Turn/step execution kernel.

## Commands

```bash
npm run build -w @mn/agent-kernel
npm run typecheck -w @mn/agent-kernel
npm run test -w @mn/agent-kernel
```

This package is private and versioned with the Muniu monorepo.
