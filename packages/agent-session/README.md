# @mn/agent-session

Durable protected sessions and local/remote persistence stores.

## Commands

```bash
npm run build -w @mn/agent-session
npm run typecheck -w @mn/agent-session
npm run test -w @mn/agent-session
```

This package is private and versioned with the Muniu monorepo.
