# ADR 0007: 受限声明式 Loop DSL

## Status

Accepted

## Decision

Loop 只能引用内置/受信 registry handler、Gate 和 Sandbox capability；远程规范包不能注入可执行代码。Repair 受轮次、时长、费用、修改范围和无进展停止条件约束。

## Consequences

Learning 只能输出提案；需要审批与 promotion 才能改变未来默认行为。
